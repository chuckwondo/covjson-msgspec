# Bridges

The free-function bridges over a `Coverage`. The numpy bridge is a pair of
methods on `NdArray` instead (see [Ranges](range.md)), since it is a small
conversion intrinsic to that leaf value type.

## xarray

::: covjson_msgspec.to_xarray

::: covjson_msgspec.from_xarray

::: covjson_msgspec.to_datatree

::: covjson_msgspec.from_datatree

## pandas

::: covjson_msgspec.to_pandas

## geo

::: covjson_msgspec.to_geojson

::: covjson_msgspec.to_geopandas
