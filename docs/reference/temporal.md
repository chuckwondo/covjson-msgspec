# Temporal

Bridge-independent conversion of CoverageJSON temporal strings: `resolve`
returns a faithful `TemporalResult` sum type, and `to_datetime` is the stdlib
convenience.

::: covjson_msgspec.to_datetime

::: covjson_msgspec.temporal.resolve

::: covjson_msgspec.temporal.TemporalResult

::: covjson_msgspec.temporal.Moment

::: covjson_msgspec.temporal.Precision

::: covjson_msgspec.temporal.Unrepresentable

::: covjson_msgspec.temporal.Malformed
